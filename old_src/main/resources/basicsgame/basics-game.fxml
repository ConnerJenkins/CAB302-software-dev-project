<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.ProgressBar?>
<?import javafx.scene.control.TextField?>
<?import javafx.scene.layout.AnchorPane?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.VBox?>

<AnchorPane xmlns="http://javafx.com/javafx/21"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="main.java.com.team.game.controller.BasicsGameController">
    <children>
        <VBox spacing="10" AnchorPane.topAnchor="12" AnchorPane.leftAnchor="12" AnchorPane.rightAnchor="12" AnchorPane.bottomAnchor="12">
            <Label text="Basics Game" style="-fx-font-size: 20px; -fx-font-weight: bold;"/>
            <HBox spacing="20">
                <Label fx:id="scoreLabel" text="Score: 0"/>
                <Label fx:id="strikesLabel" text="Strikes: 0 / 3"/>
                <Label fx:id="feedbackLabel" text="Ready..."/>
            </HBox>

            <!-- Question Display -->
            <Label fx:id="questionLabel" text="Question will appear here" style="-fx-font-size: 14px; -fx-wrap-text: true;"/>

            <!-- Multiple Choice Options -->
            <HBox spacing="8" alignment="CENTER">
                <Button fx:id="optionAButton" text="A: Option A" minWidth="120" visible="false"/>
                <Button fx:id="optionBButton" text="B: Option B" minWidth="120" visible="false"/>
            </HBox>
            <HBox spacing="8" alignment="CENTER">
                <Button fx:id="optionCButton" text="C: Option C" minWidth="120" visible="false"/>
                <Button fx:id="optionDButton" text="D: Option D" minWidth="120" visible="false"/>
            </HBox>

            <!-- Numeric Input -->
            <HBox spacing="8" alignment="CENTER">
                <Label text="Your answer:" visible="false"/>
                <TextField fx:id="numericAnswerField" promptText="Enter numeric answer" prefWidth="120" visible="false"/>
                <Button fx:id="submitNumericButton" text="Submit" visible="false"/>
            </HBox>

            <!-- Timer Progress Bar -->
            <ProgressBar fx:id="progressBar" prefWidth="400" progress="1.0"/>

            <!-- Control Buttons -->
            <HBox spacing="8" alignment="CENTER">
                <Button fx:id="nextButton" text="Next Question" onAction="#handleNextQuestion"/>
            </HBox>
        </VBox>
    </children>
</AnchorPane>